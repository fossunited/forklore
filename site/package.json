{
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "cd .. && node scripts/clean-site-output.mjs",
    "content:maintainers": "cd .. && node scripts/generate-site-maintainers.mjs",
    "build": "yarn clean && yarn content:maintainers && cd .. && site/node_modules/.bin/eleventy",
    "serve": "yarn clean && yarn content:maintainers && cd .. && site/node_modules/.bin/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
